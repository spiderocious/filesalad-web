// Icon proxy. All icon imports across the app go through `@icons`, never
// `lucide-react` directly — so the icon set is swappable in this one file.
export {
  Salad,
  UploadCloud,
  Copy,
  Check,
  Clock,
  X,
  PanelRightClose,
  PanelRightOpen,
  AlertCircle,
  Loader2,
} from 'lucide-react';
